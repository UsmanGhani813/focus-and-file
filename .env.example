# Public Supabase credentials — safe to expose in the browser bundle
SUPABASE_PROJECT_ID=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_URL=""
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Only needed for server-side admin operations (never expose to the client).
# Get this from Supabase dashboard → Settings → API Keys → Secret keys.
# SUPABASE_SERVICE_ROLE_KEY=""
